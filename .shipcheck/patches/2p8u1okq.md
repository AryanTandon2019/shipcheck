# API route may be missing authentication
This code checks for an active user session before allowing logout, preventing unauthorized access to the route.
**File:** `src/app/api/github/logout/route.ts`
```typescript
import { NextResponse } from 'next/server'; import { getSession } from 'your-auth-library'; export async function POST(req) { const session = await getSession(req); if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); // Your logic here }
```
_Generated by ShipCheck — review before merge._