# API route may be missing authentication
This code verifies the user session before processing the request, ensuring that only authenticated users can access the route.
**File:** `src/app/api/github/callback/route.ts`
```typescript
import { NextResponse } from 'next/server'; import { getSession } from 'your-auth-library'; export async function GET(req) { const session = await getSession(req); if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); // Your logic here }
```
_Generated by ShipCheck — review before merge._