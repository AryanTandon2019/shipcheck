# API route may be missing authentication
This code ensures that the user session is verified before allowing access to the API route, returning a 401 status for unauthenticated requests.
**File:** `src/app/api/github/callback/route.ts`
```typescript
import { getSession } from 'next-auth/react';

export async function GET(req, { params }) {
  const session = await getSession({ req });
  if (!session) return new Response('Unauthorized', { status: 401 });
  // Your logic here
}
```
_Generated by ShipCheck — review before merge._